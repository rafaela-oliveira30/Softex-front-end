// 1. Criando a variável com o número desejado
const numero = 7; 

// 2. Usando um laço for de 1 até 10
for (let i = 1; i <= 10; i++) {
  // 3. Mostrando cada linha da tabuada usando template string
  console.log(`${numero} x ${i} = ${numero * i}`);
}